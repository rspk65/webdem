version: 0.0
os: linux
files:
  - source: /webdem
    destination: /var/www/html/webdem
hooks:
  BeforeInstall:
    - location: scripts/start_server
      timeout: 300
      runas: root
  ApplicationStop:
    - location: scripts/stop_server
      timeout: 300
      runas: root

